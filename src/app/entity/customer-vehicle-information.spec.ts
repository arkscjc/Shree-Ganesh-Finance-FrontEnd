import { CustomerVehicleInformation } from './customer-vehicle-information';

describe('CustomerVehicleInformation', () => {
  it('should create an instance', () => {
    expect(new CustomerVehicleInformation()).toBeTruthy();
  });
});
